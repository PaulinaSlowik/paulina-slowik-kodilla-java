package com.kodilla.stream.beautifier;

public interface PoemDecorator {
    //metoda odpowiedzialna za ozdobienie tekstu przekazanego jako parametr
    public String decorate(String textForDecoration);
}
