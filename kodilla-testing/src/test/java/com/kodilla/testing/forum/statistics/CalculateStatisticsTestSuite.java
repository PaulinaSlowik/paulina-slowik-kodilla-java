/*package com.kodilla.testing.forum.statistics;

import org.junit.Test;

import java.util.List;

import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class CalculateStatisticsTestSuite {
    @Test
    public void testCalculateForumStatisticsWithMoc(){
        //Given
        Statistics forumMock = mock(Statistics.class);
        List<String> forumList = new List<String>();
        forumList.add("Maciek");
        forumList.add("Anna");
        forumList.add("Jan");
        forumList.add("Monika");
        when(forumMock.usersNames()).thenReturn(forumList);

        when(forumMock.postsCount()).thenReturn(2);

        when(forumMock.commentsCount()).thenReturn(10);

        Calculate calculate = new Calculate(forumMock);
        //When
        int

        //Then


    }
}
*/